/**
 * Re-exported from the canonical service types.
 * Add any page-specific extension types below.
 */
export type {
    Booking,
    BookingStatus,
    UseCategory,
    PaymentStatus,
} from '@/services/booking.types';
