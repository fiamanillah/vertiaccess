info:
  name: Submit Appeal
  type: http
  seq: 14

http:
  method: POST
  url: "{{baseUrl}}/users/v1/appeal"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "reason": "I have resolved the outstanding billing dispute with my bank."
      }

docs: |-
  ## Submit Appeal
  Allows a suspended user to submit an appeal to get their account reinstated.
