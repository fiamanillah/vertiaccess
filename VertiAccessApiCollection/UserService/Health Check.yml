info:
  name: Health Check
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/users/v1/health"
  headers: []

docs: |-
  ## User Service Health Check
  Checks if the user service is running.
