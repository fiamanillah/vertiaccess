info:
  name: Public Sites
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/sites/v1/public"
  headers:
    - name: Content-Type
      value: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ## Public Sites
  Returns the public list of active sites available for operator discovery.
