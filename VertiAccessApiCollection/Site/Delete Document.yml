info:
    name: Delete Document
    type: http
    seq: 12

http:
    method: DELETE
    url: '{{baseUrl}}/sites/v1/{{siteId}}/documents/{{docId}}'
    headers:
        - name: Content-Type
          value: application/json

docs: |-
    ## Delete Document
    Deletes a site document and removes the backing object from S3.
