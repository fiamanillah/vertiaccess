info:
    name: List My Sites
    type: http
    seq: 3

http:
    method: GET
    url: '{{baseUrl}}/sites/v1'
    headers:
        - name: Content-Type
          value: application/json

docs: |-
    ## List My Sites
    Fetches sites owned by the authenticated user. Admin users receive all non-deleted sites.
