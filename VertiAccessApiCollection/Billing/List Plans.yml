info:
  name: List Plans
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/billing/v1/plans"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ## List Plans

  Fetches the public list of active subscription and payg plans from the database.
