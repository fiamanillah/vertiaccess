info:
  name: Webhook
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/billing/v1/webhook"
  headers:
    - name: Content-Type
      value: application/json
    - name: Stripe-Signature
      value: "test_signature"
  body:
    type: json
    data: |-
      {}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
