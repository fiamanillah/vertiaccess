info:
  name: Health Check
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/notifications/v1/health"
  headers:
    - name: Content-Type
      value: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ## Notification Service Health Check
  Simple health check endpoint to verify the notification service is running and reachable.
