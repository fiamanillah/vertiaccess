info:
  name: Health Check
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/subscriptions/v1/health"
  headers: []

docs: |-
  ## Subscription Service Health Check
  Checks if the subscription service is running.
