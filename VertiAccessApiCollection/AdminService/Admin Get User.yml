info:
  name: Admin Get User
  type: http
  seq: 18

http:
  method: GET
  url: "{{baseUrl}}/admin/v1/users/:id"
  headers: []

docs: |-
  ## Admin: Get User
  Retrieves detailed profile, subscription status, activity counters, and metadata for a specific user ID.
  Requires admin privileges.
