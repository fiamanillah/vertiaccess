info:
    name: Admin Analytics
    type: http
    seq: 13

http:
    method: GET
    url: '{{baseUrl}}/admin/v1/analytics'
    headers:
        - name: Content-Type
          value: application/json
        - name: Authorization
          value: 'Bearer {{idToken}}'

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5

docs: |-
    ## Admin Analytics

    Retrieves detailed analytics data for administrative dashboards including trends, usage patterns, and engagement metrics.

    **Admin Only**
