info:
  name: Admin List Verifications
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/admin/v1/verifications"
  headers:
    - name: Authorization
      value: Bearer {{token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ## Admin: List Verifications
  Returns a list of all pending and historical verification requests (Identity and Operator).
  Requires admin privileges.
