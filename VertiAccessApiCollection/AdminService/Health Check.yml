info:
  name: Health Check
  type: http
  seq: 25

http:
  method: GET
  url: "{{baseUrl}}/admin/v1/health"
  headers: []

docs: |-
  ## Admin Service Health Check
  Checks if the admin service is running.
