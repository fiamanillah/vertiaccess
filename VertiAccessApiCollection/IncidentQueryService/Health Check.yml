info:
  name: Health Check
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/incident-queries/v1/health"
  headers: []

docs: |-
  ## Incident Query Service Health Check
  Checks if the incident query service is running.
