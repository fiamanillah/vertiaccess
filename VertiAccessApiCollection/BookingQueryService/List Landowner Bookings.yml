info:
  name: List Landowner Bookings
  type: http
  seq: 16

http:
  method: GET
  url: "{{baseUrl}}/booking-queries/v1/landowner"
  headers:
    - name: Authorization
      value: "Bearer {{auth_token}}"

docs: |-
  ## List Landowner Bookings
  Returns all bookings across all sites owned by the authenticated landowner.
