info:
  name: Get Booking Timeline
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/booking-queries/v1/:bookingId/timeline"
  headers: []

docs: |-
  ## Get Booking Timeline
  Returns the lifecycle timeline of events for a booking. Accessible by operator, landowner, or admin.
