info:
  name: List My Bookings
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/booking-queries/v1/mine"
  headers:
    - name: Authorization
      value: "Bearer {{auth_token}}"

docs: |-
  ## List My Bookings
  Returns all bookings created by the authenticated operator.
