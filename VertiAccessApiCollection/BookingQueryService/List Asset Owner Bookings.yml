info:
  name: List AssetOwner Bookings
  type: http
  seq: 16

http:
  method: GET
  url: "{{baseUrl}}/booking-queries/v1/assetowner"
  headers:
    - name: Authorization
      value: "Bearer {{auth_token}}"

docs: |-
  ## List AssetOwner Bookings
  Returns all bookings across all sites owned by the authenticated assetowner.
