info:
  name: Get Booking Details
  type: http
  seq: 19

http:
  method: GET
  url: "{{baseUrl}}/booking-queries/v1/:bookingId"
  headers:
    - name: Authorization
      value: "Bearer {{auth_token}}"

vars:
  bookingId: 

docs: |-
  ## Get Booking Details
  Returns detailed information about a single booking.
