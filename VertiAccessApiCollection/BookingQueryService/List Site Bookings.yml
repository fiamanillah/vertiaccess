info:
  name: List Site Bookings
  type: http
  seq: 17

http:
  method: GET
  url: "{{baseUrl}}/booking-queries/v1/site/:siteId"
  headers:
    - name: Authorization
      value: "Bearer {{auth_token}}"

vars:
  siteId: 

docs: |-
  ## List Site Bookings
  Returns all bookings for a specific site. Requires ownership or admin privileges.
