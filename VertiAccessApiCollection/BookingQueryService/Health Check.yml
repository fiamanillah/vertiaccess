info:
  name: Health Check
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/booking-queries/v1/health"
  headers: []

docs: |-
  ## Booking Query Service Health Check
  Checks if the booking query service is running.
