info:
  name: Get Booking Certificate
  type: http
  seq: 18

http:
  method: GET
  url: "{{baseUrl}}/booking-queries/v1/:bookingId/certificate"
  headers:
    - name: Authorization
      value: "Bearer {{auth_token}}"

vars:
  bookingId: 

docs: |-
  ## Get Booking Certificate
  Returns the digital consent certificate data for a specific booking.
