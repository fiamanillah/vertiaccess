info:
  name: List AssetManager Bookings
  type: http
  seq: 16

http:
  method: GET
  url: "{{baseUrl}}/booking-queries/v1/assetmanager"
  headers:
    - name: Authorization
      value: "Bearer {{auth_token}}"

docs: |-
  ## List AssetManager Bookings
  Returns all bookings across all sites owned by the authenticated assetmanager.
