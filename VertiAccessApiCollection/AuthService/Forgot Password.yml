info:
  name: Forgot Password
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/auth/v1/forgot-password"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "mdbitu35@gmail.com"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ## Forgot Password
  Initiates the password reset flow by sending a 6-digit reset code to the user's email.
