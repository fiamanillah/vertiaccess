info:
  name: Health Check
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/auth/v1/health"
  headers:
    - name: Content-Type
      value: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  ## Auth Service Health Check
  Simple health check endpoint to verify the auth service is running and reachable.
