info:
    name: Get Site
    type: http
    seq: 4

http:
    method: GET
    url: '{{baseUrl}}/sites/v1/{{siteId}}'
    headers:
        - name: Content-Type
          value: application/json

docs: |-
    ## Get Site
    Fetches a single site record by ID for the authenticated owner or admin.
