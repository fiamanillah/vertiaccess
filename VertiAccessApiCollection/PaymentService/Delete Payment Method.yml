info:
  name: Delete Payment Method
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{baseUrl}}/payments/v1/payment-methods/:paymentMethodId"
  headers:
    - name: Authorization
      value: "Bearer {{auth_token}}"

vars:
  paymentMethodId: 

docs: |-
  ## Delete Payment Method
  Detaches a payment method from the user's customer record.
