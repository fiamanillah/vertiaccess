info:
  name: Upload File Proxy
  type: http
  seq: 13

http:
  method: PUT
  url: "{{baseUrl}}/documents/v1/upload-file"
  headers:
    - name: Authorization
      value: "Bearer {{auth_token}}"

docs: |-
  ## Upload File Proxy
  Proxies a file upload to S3 using a pre-signed URL or direct upload path.
