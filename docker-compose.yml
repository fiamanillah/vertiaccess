services:
  postgres:
    image: postgis/postgis:16-3.4
    container_name: vertiaccess-db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: vertiaccess
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
